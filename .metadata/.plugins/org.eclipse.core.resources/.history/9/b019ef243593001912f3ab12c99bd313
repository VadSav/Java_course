import java.util.Arrays;
import java.util.Comparator;

public class Demo {
	public static void main(String[] args) {
		Lines s1 = new Lines ("not in money happiness");
		Lines s2 = new Lines ("a good car is expensive");
		Lines s3 = new Lines ("i love sports");
		Lines lines [] = {s1,s2,s3};
		
		Arrays.sort(args);
	
		 Comparator<Lines> comp = new ComparatorByLength();
	        Arrays.sort(lines, comp.thenComparing(new ComparatorByLength()));
	        
	        for (int i = 0; i < lines.length; i++) {
	            System.out.println(lines[i]);
	        }
		
	        
	}

	       // public  static void temp(String str){
	        
	        
	       // }

		
	      

	Countable c=(w1,w2)->{
		if(w1>w2) {
			return w1;
		}else {
			return w2;
		}
			};	//  toje samoe po drugomu
	Countable c2 =(w1,w2)-> w1>w2 ? w1:w2;
	int count = c.count(1,2);
			
		}

 

